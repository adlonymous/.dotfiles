---
description: Draft a clear git commit message from current changes
---
Inspect the current git changes and draft a commit message.

Return:
- One conventional-commit style subject line
- Optional body bullets if useful
- Any concerns before committing

Do not create the commit unless I explicitly ask.
